{
  "name": "keyflow",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:desktop": "node scripts/build-desktop.mjs",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:mac": "CI=true tauri build --bundles app,dmg",
    "desktop:mac:universal": "CI=true tauri build --target universal-apple-darwin --bundles app,dmg",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
