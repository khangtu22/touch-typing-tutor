{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the bundled KeyFlow window can access core desktop APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
